# 🚀 Быстрый старт

## Минимальный запуск (3 команды)

```bash
# 1. Генерация SSL сертификатов (опционально, можно пропустить)
# Linux/Mac:
cd nginx && ./generate-cert.sh && cd ..

# 2. Запуск всех сервисов
docker-compose up -d

# 3. Подождите 30 секунд, затем примените миграции и создайте админа
docker-compose exec backend alembic revision --autogenerate -m "Initial"
docker-compose exec backend alembic upgrade head
docker-compose exec backend python -m app.scripts.create_admin
```

Готово! Откройте https://localhost или http://localhost

---

## 📋 Тестовые аккаунты

После выполнения скрипта `create_admin` у вас будут:

| Username | Password | Роль |
|----------|----------|------|
| admin | Admin123! | Administrator |
| staff_user | Staff123! | Staff |
| regular_user | User123! | User |

---

## 🧪 Что можно протестировать

### 1. Регистрация и вход
- Зарегистрируйтесь с новым аккаунтом
- Войдите через форму логина

### 2. Включение 2FA
- Профиль → Включить 2FA
- Сканируйте QR код в Google Authenticator
- Подтвердите код
- Выйдите и войдите снова с 2FA

### 3. Админ-панель (login: admin)
- Просмотр пользователей
- Создание нового пользователя
- Блокировка/разблокировка
- Сброс 2FA
- Просмотр логов с фильтрами

### 4. Каталог товаров
- Просмотр без авторизации
- Фильтрация по категориям

---

## 📊 Проверка логирования

Все действия логируются в БД:
1. Войдите как admin
2. Откройте Админ-панель → Журнал событий
3. Увидите все события:
   - Входы/выходы
   - Регистрации
   - Включение 2FA
   - Создание пользователей
   - Просмотр каталога
   - И многое другое

Фильтруйте по:
- Пользователю
- Типу операции
- Статусу (success/failed)
- Роли
- Сортировка

---

## 🔧 Полезные команды

```bash
# Логи
docker-compose logs -f backend
docker-compose logs -f frontend

# Перезапуск
docker-compose restart backend
docker-compose restart frontend

# Остановка
docker-compose stop

# Остановка + удаление (БД сохранится)
docker-compose down

# Полная очистка (включая БД)
docker-compose down -v
```

---

## 📁 Основные URL

- **Frontend**: https://localhost
- **Backend API**: http://localhost:8000
- **API Docs (Swagger)**: http://localhost:8000/docs
- **Database**: localhost:5432

---

## ⚠️ SSL Предупреждение

При открытии https://localhost браузер предупредит о небезопасном сертификате. Это нормально для разработки (самоподписанный сертификат).

Нажмите:
- Chrome: "Advanced" → "Proceed to localhost"
- Firefox: "Advanced" → "Accept the Risk and Continue"

Или используйте HTTP: http://localhost (без 's')

---

## 🎯 Требования ТЗ

Все требования реализованы:

✅ FastAPI backend  
✅ React frontend  
✅ PostgreSQL  
✅ Docker окружение  
✅ JWT аутентификация (httpOnly cookies)  
✅ 2FA TOTP (Google Authenticator)  
✅ RBAC (admin/staff/user)  
✅ Полное логирование (audit_log)  
✅ Админ-панель  
✅ Управление пользователями  
✅ Просмотр логов с фильтрами  
✅ HTTPS/TLS  
✅ Bcrypt пароли  
✅ Валидация паролей  
✅ CORS политика  
✅ Alembic миграции  

---

**Готово к защите курсовой! 🎓**

