# 🔓 Запуск без SSL (HTTP режим)

Если у вас нет OpenSSL или вы хотите запустить систему без HTTPS, следуйте этой инструкции.

## Автоматический способ

Скрипт `START_WINDOWS.bat` автоматически определит отсутствие OpenSSL и переключится на HTTP режим.

## Ручной способ

### Вариант 1: Использовать HTTP конфигурацию nginx

```bash
# Скопируйте HTTP конфигурацию
copy nginx\nginx.conf.http nginx\nginx.conf

# Запустите контейнеры
docker-compose up -d
```

### Вариант 2: Работать без nginx (напрямую)

Если nginx не нужен, можно работать напрямую с backend и frontend:

```bash
# Запустите только необходимые сервисы
docker-compose up -d postgres backend frontend

# Доступ:
# Frontend: http://localhost:3000
# Backend:  http://localhost:8000
# API Docs: http://localhost:8000/docs
```

**Важно:** При работе без nginx нужно обновить `frontend/src/services/api.ts`:

```typescript
const API_URL = 'http://localhost:8000'; // вместо через nginx
```

## Что изменится

### ✅ Работает:
- Все функции системы
- Аутентификация
- 2FA
- Админ-панель
- Логирование

### ⚠️ Отличия:
- Нет HTTPS шифрования (только HTTP)
- Cookies будут работать, но без Secure flag
- Браузер не будет предупреждать о сертификате
- Для продакшена все равно нужен HTTPS

## Установка OpenSSL (опционально)

Если хотите использовать HTTPS:

### Windows:

1. **Через Git for Windows** (рекомендуется):
   - Установите: https://git-scm.com/download/win
   - OpenSSL будет доступен в Git Bash

2. **Через Chocolatey**:
   ```powershell
   choco install openssl
   ```

3. **Через Scoop**:
   ```powershell
   scoop install openssl
   ```

4. **Вручную**:
   - Скачайте: https://slproweb.com/products/Win32OpenSSL.html
   - Добавьте в PATH

### После установки OpenSSL:

```bash
cd nginx
generate-cert.bat
cd ..
copy nginx\nginx.conf nginx\nginx.conf  # вернуть HTTPS конфиг
docker-compose restart nginx
```

## Проверка режима

После запуска проверьте:

```bash
# Если есть SSL сертификаты
dir nginx\ssl\cert.crt

# Если файл существует - HTTPS режим
# Если нет - HTTP режим
```

## Рекомендации

- **Для разработки**: HTTP режим вполне достаточен
- **Для демонстрации**: Можно использовать HTTP, но лучше HTTPS
- **Для продакшена**: Обязательно нужен валидный SSL сертификат (Let's Encrypt)

---

**Система полностью функциональна в обоих режимах!** ✅

