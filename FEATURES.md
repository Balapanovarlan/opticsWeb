# 🔐 Возможности системы защиты информации

## Аутентификация и авторизация

### ✅ JWT Токены
- Access token (30 минут)
- Refresh token (7 дней)
- Хранение в HttpOnly cookies (защита от XSS)
- Автоматическое обновление через interceptor

**Файлы:**
- `backend/app/core/security.py` - создание и проверка токенов
- `backend/app/api/auth.py` - endpoints регистрации/входа
- `frontend/src/services/api.ts` - interceptor для refresh

### ✅ Двухфакторная аутентификация (2FA)
- TOTP алгоритм (Google Authenticator)
- QR код для быстрой настройки
- Подтверждение при включении
- Требование пароля для отключения
- Сброс администратором при необходимости

**Файлы:**
- `backend/app/auth/totp.py` - логика TOTP
- `backend/app/api/twofa.py` - endpoints 2FA
- `frontend/src/pages/ProfilePage.tsx` - UI настройки

### ✅ Валидация паролей
- Минимум 8 символов
- Обязательно: цифра, строчная, заглавная буквы
- Рекомендуются: специальные символы
- Bcrypt хэширование (cost factor: 12)

**Файлы:**
- `backend/app/core/security.py` - функция `validate_password_strength`

---

## Управление доступом (RBAC)

### ✅ Роли пользователей
1. **Administrator** - полный доступ ко всем функциям
2. **Staff** - доступ к админ-панели (ограниченный)
3. **User** - обычный пользователь

### ✅ Права доступа
```
┌─────────────────┬───────┬───────┬──────┐
│ Функция         │ Admin │ Staff │ User │
├─────────────────┼───────┼───────┼──────┤
│ Каталог         │   ✓   │   ✓   │  ✓   │
│ Регистрация     │   ✓   │   ✓   │  ✓   │
│ Профиль         │   ✓   │   ✓   │  ✓   │
│ 2FA настройки   │   ✓   │   ✓   │  ✓   │
│ Админ-панель    │   ✓   │   ✓   │  ✗   │
│ Просмотр логов  │   ✓   │   ✓   │  ✗   │
│ Создание юзеров │   ✓   │   ✗   │  ✗   │
│ Удаление юзеров │   ✓   │   ✗   │  ✗   │
│ Изменение ролей │   ✓   │   ✗   │  ✗   │
│ Блокировка      │   ✓   │   ✗   │  ✗   │
└─────────────────┴───────┴───────┴──────┘
```

**Файлы:**
- `backend/app/auth/dependencies.py` - проверка ролей
- `backend/app/db/models/user.py` - enum UserRole
- `frontend/src/App.tsx` - ProtectedRoute компонент

---

## Логирование и аудит

### ✅ Типы событий
**Аутентификация:**
- `login_success` - успешный вход
- `login_failed` - неудачная попытка входа
- `logout` - выход
- `2fa_enabled` - включение 2FA
- `2fa_disabled` - отключение 2FA
- `2fa_failed` - ошибка TOTP кода

**Пользователи:**
- `registration` - регистрация
- `user_created` - создание администратором
- `user_updated` - обновление данных
- `user_deleted` - удаление
- `user_blocked` / `user_unblocked` - блокировка
- `role_changed` - изменение роли

**Данные:**
- `product_view` - просмотр каталога/товара
- `logs_viewed` - просмотр журнала событий
- `forbidden_access` - попытка доступа к запрещенному ресурсу

### ✅ Информация в логах
Каждая запись содержит:
- **timestamp** - точное время события
- **user_id** - ID пользователя (NULL если не аутентифицирован)
- **username** - имя пользователя или введенный логин
- **role** - роль на момент события
- **operation** - тип операции
- **target_table** - таблица/сущность (если применимо)
- **target_id** - ID записи (если применимо)
- **status** - success / failed / warning
- **ip_address** - IP адрес клиента
- **details** - текстовое описание

### ✅ Просмотр логов
**Фильтрация:**
- По диапазону дат
- По пользователю
- По типу операции
- По статусу
- По роли
- По IP адресу

**Сортировка:**
- По времени (ASC/DESC)
- По пользователю
- По операции
- По статусу

**Пагинация:**
- Настраиваемое количество записей на странице
- Навигация по страницам

**Файлы:**
- `backend/app/db/models/audit_log.py` - модель
- `backend/app/middleware/logging.py` - функция логирования
- `backend/app/api/logs.py` - API просмотра
- `frontend/src/pages/admin/AdminLogsPage.tsx` - UI

---

## Защита данных

### ✅ Шифрование
- **HTTPS/TLS** - весь трафик шифруется
- **Bcrypt** - хэширование паролей (cost: 12)
- **TOTP секреты** - хранятся в БД в зашифрованном виде

### ✅ Защита токенов
- **HttpOnly cookies** - недоступны для JavaScript
- **Secure flag** - только HTTPS
- **SameSite=Lax** - защита от CSRF
- **Короткий срок жизни** - Access token 30 минут

### ✅ Защита от атак
- **SQL Injection** - параметризованные запросы (SQLAlchemy ORM)
- **XSS** - HttpOnly cookies, санитизация входных данных
- **CSRF** - SameSite cookies, CORS политика
- **Rate Limiting** - ограничение попыток входа (ready to implement)
- **Brute Force** - блокировка после N неудачных попыток (ready)

**Файлы:**
- `nginx/nginx.conf` - HTTPS, заголовки безопасности
- `backend/app/core/security.py` - хэширование, токены
- `backend/app/main.py` - CORS middleware

---

## Администрирование

### ✅ Управление пользователями
- Просмотр списка всех пользователей
- Создание новых пользователей с выбором роли
- Редактирование email, статуса
- Блокировка/разблокировка аккаунтов
- Изменение ролей
- Сброс 2FA
- Удаление пользователей (с защитой от самоудаления)

### ✅ Дашборд
- Общая статистика пользователей
- Распределение по ролям
- Количество с включенным 2FA
- Статистика событий
- Топ операций
- События по статусам

**Файлы:**
- `backend/app/api/admin.py` - API администрирования
- `frontend/src/pages/admin/AdminUsersPage.tsx` - управление
- `frontend/src/pages/admin/AdminDashboard.tsx` - статистика

---

## База данных

### ✅ Структура
**Таблица users:**
```sql
- id (PK)
- username (unique, indexed)
- email (unique, indexed)
- password_hash
- role (enum: admin, staff, user)
- is_2fa_enabled
- secret_2fa
- is_active
- is_blocked
- created_at
- updated_at
- last_login
```

**Таблица audit_log:**
```sql
- id (PK)
- timestamp (indexed)
- user_id (nullable)
- username
- role
- operation (enum, indexed)
- target_table
- target_id
- status (enum, indexed)
- ip_address
- details (text)
```

### ✅ Миграции
- Alembic для управления схемой
- Автоматическая генерация миграций
- Версионирование изменений БД
- Откат к предыдущим версиям

**Файлы:**
- `backend/app/db/models/` - модели SQLAlchemy
- `backend/alembic/` - миграции

---

## Интерфейс

### ✅ Публичная часть
- **Главная страница** - описание системы, возможности
- **Каталог товаров** - просмотр без авторизации, фильтры
- **Регистрация** - с валидацией пароля
- **Вход** - с поддержкой 2FA

### ✅ Личный кабинет
- **Профиль** - основная информация
- **Настройка 2FA** - включение/отключение, QR код
- **История входов** - last_login

### ✅ Админ-панель
- **Дашборд** - статистика системы
- **Пользователи** - CRUD, блокировка, роли
- **Журнал событий** - просмотр, фильтры, пагинация
- **Sidebar навигация** - удобное переключение

### ✅ UX/UI
- Современный дизайн (Tailwind CSS)
- Адаптивная верстка (responsive)
- Индикаторы загрузки
- Toast уведомления об ошибках/успехе
- Защищенные маршруты с редиректами
- Подтверждение критических действий

**Файлы:**
- `frontend/src/pages/` - все страницы
- `frontend/src/components/layouts/` - layout'ы
- `frontend/src/styles/index.css` - стили

---

## API Documentation

Автоматическая документация Swagger UI доступна по адресу:
- http://localhost:8000/docs (Swagger)
- http://localhost:8000/redoc (ReDoc)

### Эндпоинты:

**Аутентификация:**
```
POST /auth/register
POST /auth/login
POST /auth/logout
POST /auth/refresh
GET  /auth/me
```

**2FA:**
```
POST /2fa/enable
POST /2fa/verify
POST /2fa/disable
```

**Администрирование (admin only):**
```
GET    /admin/users
GET    /admin/users/{id}
POST   /admin/users
PUT    /admin/users/{id}
DELETE /admin/users/{id}
PUT    /admin/users/{id}/role
POST   /admin/users/{id}/reset-2fa
```

**Логи (admin/staff):**
```
GET /admin/logs
GET /admin/logs/{id}
GET /admin/logs/stats/summary
```

**Каталог (public):**
```
GET /products
GET /products/{id}
```

---

## Развертывание

### ✅ Docker
- **Мультистейдж сборка** - оптимизация размера образов
- **Health checks** - проверка готовности PostgreSQL
- **Volume mapping** - персистентность данных
- **Network isolation** - изоляция сервисов
- **Environment variables** - гибкая конфигурация

### ✅ Nginx
- **Reverse proxy** - единая точка входа
- **HTTPS termination** - TLS шифрование
- **Load balancing** - готовность к масштабированию
- **Security headers** - HSTS, X-Frame-Options, etc.

### ✅ CI/CD Ready
- Готово к интеграции с GitHub Actions
- Docker Compose для локальной разработки
- Production-ready конфигурация

---

## Соответствие стандартам

### ✅ ГОСТ Р 50739-95
- Защита от НСД
- Идентификация и аутентификация
- Регистрация и учет
- Контроль целостности

### ✅ Требования СЗИ
- Аутентификация пользователей
- Контроль доступа (RBAC)
- Регистрация событий
- Защита информации при хранении и передаче
- Обеспечение целостности
- Антивирусная защита (на уровне инфраструктуры)

---

## Готовность к продакшену

### ✅ Реализовано
- Логирование в файлы и БД
- Обработка ошибок
- Валидация входных данных
- Миграции БД
- Environment variables
- Docker окружение
- HTTPS/TLS

### 🔜 Рекомендуется добавить
- Мониторинг (Prometheus + Grafana)
- Бэкапы базы данных
- Rate limiting middleware
- Email уведомления
- Восстановление пароля
- Captcha при регистрации
- Session management
- IP whitelist для админов

---

**Система полностью готова для защиты курсовой работы! 🎓✅**

