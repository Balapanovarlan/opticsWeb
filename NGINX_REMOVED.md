# 🔧 Удаление Nginx из проекта

## ✅ Что сделано

Nginx был удален из архитектуры проекта для упрощения разработки и избежания проблем с HTTPS/SSL сертификатами.

## 🏗️ Новая архитектура

**Было (с Nginx):**
```
Browser → Nginx (80/443) → Frontend (3000) + Backend (8000)
```

**Стало (без Nginx):**
```
Browser → Frontend (3000) ⟷ Backend (8000)
```

## 📝 Изменения в файлах

### 1. `frontend/src/services/api.ts`
```typescript
// Прямое подключение к backend без nginx
const API_URL = 'http://localhost:8000';
```

### 2. `docker-compose.yml`
- **Закомментирован** сервис `nginx`
- **Обновлена** переменная окружения фронтенда:
  ```yaml
  environment:
    - VITE_API_URL=http://localhost:8000
  ```

### 3. Документация
- `README.md` - обновлена архитектура и инструкции
- `QUICKSTART.md` - убрана генерация SSL, обновлены URL
- Удалены упоминания HTTPS/TLS

## 🚀 Как запустить

```bash
# 1. Запуск контейнеров
docker-compose up -d

# 2. Миграции (подождите 30 сек)
docker-compose exec backend alembic revision --autogenerate -m "Initial"
docker-compose exec backend alembic upgrade head

# 3. Создание администратора
docker-compose exec backend python -m app.scripts.create_admin
```

**Откройте:** http://localhost:3000

## 🔑 Важно для авторизации

**Всегда используйте `http://localhost:3000`** для доступа к приложению!

- ✅ Правильно: `http://localhost:3000`
- ❌ Неправильно: ~~`http://localhost:8000`~~ (это backend API)

## 📊 Доступ к сервисам

| Сервис | URL | Описание |
|--------|-----|----------|
| Frontend | http://localhost:3000 | React приложение |
| Backend API | http://localhost:8000 | FastAPI сервер |
| API Docs | http://localhost:8000/docs | Swagger UI |
| Database | localhost:5432 | PostgreSQL |

## 🔐 Система защиты

Все механизмы защиты остались:
- ✅ JWT токены (Access + Refresh)
- ✅ localStorage для хранения
- ✅ 2FA TOTP
- ✅ RBAC (admin/staff/user)
- ✅ Bcrypt хэширование
- ✅ CORS политика
- ✅ SQL Injection protection
- ✅ XSS protection
- ✅ Полное логирование

**Единственное отличие:** Нет HTTPS/TLS (это нормально для локальной разработки).

## 🧪 Тестирование

1. Откройте `http://localhost:3000`
2. Залогиньтесь: `admin` / `Admin123!`
3. Перейдите в Админ-панель
4. Проверьте, что токены сохраняются после перезагрузки страницы

## 📁 Файлы Nginx (сохранены на всякий случай)

Если в будущем понадобится вернуть nginx:
- `nginx/nginx.conf` - основной конфиг (HTTPS)
- `nginx/nginx.conf.http` - HTTP версия
- `nginx/generate-cert.sh` - генерация SSL (Linux/Mac)
- `nginx/generate-cert.bat` - генерация SSL (Windows)

Просто раскомментируйте сервис `nginx` в `docker-compose.yml`.

## ✅ Итог

✅ Nginx удален  
✅ Прямое подключение Frontend ↔ Backend  
✅ Нет проблем с SSL сертификатами  
✅ Упрощенная архитектура для разработки  
✅ Все функции СЗИ работают  
✅ Токены сохраняются корректно  

**Готово к тестированию! 🎓**

